1class Solution {
2public:
3    int minimumIndex(vector<int>& capacity, int itemSize) {
4        int n = capacity.size();
5
6        int mn = INT_MAX;
7        int idx = -1;
8
9        for (int i = 0; i < n; i++) {
10            if (capacity[i] < mn && capacity[i]>=itemSize) {
11                mn = capacity[i];
12                idx = i;
13            }
14        }
15
16        return idx;
17    }
18};